{
  "id": "mcp-caldav",
  "name": "CalDAV MCP bridge",
  "description": "Protocol-level CalDAV MCP bridge bound to an app-specific password. Two tools (list_events, get_event) implemented over RFC 4791 CalDAV (REPORT calendar-query / PROPFIND). Provider-agnostic; accepts any IETF-compliant CalDAV host.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "tools": ["list_events", "get_event"],
  "env": {
    "required": ["ENCLAWED_IMAP_APP_PASSWORD", "ENCLAWED_SECRETARY_PRINCIPAL_EMAIL"]
  },
  "endpoints": {
    "synthetic": "mcp+caldav://"
  }
}
