import type { EnclawedConfig } from "../../../config/types.enclawed.js";
export declare function normalizeRuntimeCompatibilityConfigValues(cfg: EnclawedConfig): {
    config: EnclawedConfig;
    changes: string[];
};
